(ns bluegenes.toolmap)

(def tools
  {"runtemplate" {:title "Structured Search"
                  :accepts {:type "Gene"}}
   "viewtable" {:title "Inspect Results"
                :accepts {:type "Gene"}}
   })
